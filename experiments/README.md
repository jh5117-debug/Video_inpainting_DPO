# Experiment Runs

This directory is reserved for run-time artifacts such as per-experiment run folders, manifests, logs, and checkpoints. Code for each experiment lives in top-level experiment-named packages, not here.

Do not commit run outputs. Use `EXP_OUTPUT_ROOT` for machine-specific output storage.
