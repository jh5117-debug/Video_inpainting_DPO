# Exp20 Commands

```bash
python -m unittest discover -s exp20_autoresearch_scale_adaptive_region_dpo/tests
python exp20_autoresearch_scale_adaptive_region_dpo/code/search_controller.py --init-roots
```

Heavy training remains disabled until parity and locked dev split reports pass.
