experiment_name: diffueraser_reproduction_sft
status: completed_protected

evaluation:
  denoise_steps: 6
  use_pcm: false
  gaussian_blur_after_composite: false
  metric_transport: frames

paths:
  data_root_env: YOUTUBE_VOS_ROOT
  weight_root_env: DIFFUERASER_WEIGHT_ROOT
  output_root_env: EXP_OUTPUT_ROOT
