experiment_id: exp01
experiment_name: DiffuEraser reproduction / SFT / metric setting
status: historical_complete_registry_partial
data_asset: DiffuEraser official/SFT assets
source_domain: DAVIS/YouTube-VOS-style inpainting
target_domain: video inpainting
manifest: historical; see PRD/DPO_Project_Complete_Summary.md
train_task: DiffuEraser SFT/reproduction
train_mask_mode: partial/full depending eval
mask_from_manifest: n/a
mask_area_range: n/a
prior_mode: propainter for partial-mask inpainting; no-prior/noise for fullmask generation
beta_dpo: 
lose_gap_weight: 
sft_reg_weight: SFT MSE
winner_abs_reg_weight: 
winner_gap_reg_weight: 
winner_gap_reg_margin: 
loss_region_mode: SFT reconstruction
stage1_run_dir: historical path backfill needed
stage2_run_dir: historical path backfill needed
dpo_diag_csv: not DPO
qual_dir: historical qualitative path backfill needed
metric_dir: historical metrics path backfill needed
vbench_dir: n/a for inpainting
conclusion: SFT-48000 is a strong YouTube-VOS-tuned baseline, not a naked base.
loss_formula: SFT reconstruction MSE, not DPO.
