# Experiment Registry: Exp15 OR Benchmark

Exp15 is the object-removal benchmark line. It compares frozen OR/inpainting
baselines on DAVIS50 true foreground masks plus YouTubeVOS100.

No training is part of this experiment.

Current status: dataset staged, manifests ready, baseline runtime feasibility
partially verified.
