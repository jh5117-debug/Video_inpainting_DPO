# Metrics Summary

Metric directory: `needs artifact backfill`

Metric policy:

- Video generation / full-mask generation: VBench where applicable.
- Video inpainting / partial-mask inpainting: project inpainting metrics through `inference/metrics.py` wrappers.

Current metric conclusion: Showed the DiffuEraser bridge can run; later failures are not just launcher failure.
