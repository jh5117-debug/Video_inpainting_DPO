# Metric Summary

Metric path: `path backfill needed`

Conclusion: DiffuEraser can be wired into VideoDPO; later failures are objective/data issues.

For partial-mask inpainting use `inference/metrics.py` / wrapper, not VBench.
