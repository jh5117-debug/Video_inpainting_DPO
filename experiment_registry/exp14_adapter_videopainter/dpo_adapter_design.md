# DPO Adapter Design

Use the same design as:

```text
exp14_adapter_videopainter/dpo_adapter_design.md
```

Core setting:

```text
region-local normalized-gap clipped-loser-gap winner-anchored DPO
boundary_mode = outer
boundary_weight = 0.75
```

