# Exp58 VOID Native Data Diagnostic

Exp58 investigates whether VOID adapter failures are caused by VOR-derived data mismatch. It uses official VOID data-generation code paths where possible and does not modify VOID official source, shared trainer code, or `inference/metrics.py`.
