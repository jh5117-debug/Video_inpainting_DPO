# Exp22 Status

Status: ASSET_SCAN_SCAFFOLD_READY

The scanner records discovered repos/weights/env clues and emits explicit
BLOCKED/PENDING statuses. It must not silently skip models or create fake smoke
results.
