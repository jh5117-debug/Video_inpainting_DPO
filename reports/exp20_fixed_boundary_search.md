# Exp20 Fixed Boundary Search

Status: `NOT_STARTED_PARITY_GATE`

Fixed boundary search has not started. The code-level preflight and unit tests
pass, but full trainer parity against Exp11 on the same checkpoint, batch, noise,
timestep, and latent mask is still required before any PAI GPU search is allowed.

Root trial configs have been generated under:

```text
exp20_autoresearch_scale_adaptive_region_dpo/configs/
```

Queued root count: 18.
