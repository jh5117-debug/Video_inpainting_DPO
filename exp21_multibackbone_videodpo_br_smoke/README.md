# Exp21: Multibackbone VideoDPO BR Plumbing Smoke

Compatibility-only smoke for diffusion/video inpainting backbones. This folder
does not claim quality results. Each backend must pass real checkpoint load,
policy/reference forward, finite DPO loss, finite gradients, save/reload, and
adapter inference before it can be marked `READY_SMOKE_PASSED`.
