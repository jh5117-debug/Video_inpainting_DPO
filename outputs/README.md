# Outputs Directory

Runtime outputs, generated videos, logs, VBench results, and qualitative SBS videos belong here or under `EXP_OUTPUT_ROOT`.

Do not commit generated outputs. Keep only this README and lightweight placeholders.

Asset smoke tests use:

```text
outputs/asset_smoke_tests/<timestamp>/
```
