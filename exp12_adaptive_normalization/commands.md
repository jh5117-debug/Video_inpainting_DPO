# Commands

Start through the master launcher:

```bash
bash scripts/launch_exp11_exp12_parallel_pai.sh
```

The Exp12 worker uses GPUs `4,5,6,7` and launches only `exp12_batch_adaptive_norm_s1s2_2000`.
